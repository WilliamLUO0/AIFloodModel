# general settings
name: 002_FMPFT_SRx16_scratch
model_type: FMSRModel
scale: 16
num_gpu: 1
manual_seed: 61

# dataset and data loader settings
datasets:
  test_1:
    name: FloodMapSR-Train-h-100y42h0c
    type: PairedFloodMapDataset
    dataroot: /nesi/nobackup/uoa04425/zluo784/Exp1/AIFloodModel/dataset_test_100y42h0c
    index_csv: /nesi/nobackup/uoa04425/zluo784/Exp1/AIFloodModel/dataset_test_100y42h0c/index.csv
    io_backend:
      type: disk

    phase: test
    target_var: h
    scale: 16
    patch_fine: 1024
    patch_coarse: 64

    split_cfg:
      by: scenario
      val_ratio: 0.2
      seed: 61
      split_stats_json: /nesi/nobackup/uoa04425/zluo784/Exp1/AIFloodModel/dataset/split_stats_h.json
    stats:
      calculate_if_missing: false
      use_clip_coarse: true
      use_clip_static: true

# network structures
network_g:
  type: FloodMapPFT
  upscale: 16
  coarse_in_chans: 1
  static_in_chans: 7
  flood_map_size: 64
  use_shallow_act: true
  use_aoi_gate: true
  aoi_alpha: 0.8
  embed_dim: 240
  depths: [4, 4, 4, 6, 6, 6]
  num_heads: 6
  num_topk: [ 1024, 1024, 1024, 1024,
              256, 256, 256, 256,
              128, 128, 128, 128,
              64, 64, 64, 64, 64, 64,
              32, 32, 32, 32, 32, 32,
              16, 16, 16, 16, 16, 16,]
  window_size: 32
  convffn_kernel_size: 7
  mlp_ratio: 2.
  upsampler: 'pixelshuffle'
  resi_connection: '1conv'
  use_checkpoint: false

# path
path:
  pretrain_network_g: experiments/pretrained_models/002_FMPFT_SRx16_scratch.pth
  strict_load_g: yes
  param_key_g: params_ema

# validation settings
val:
  save_flood_map: true
  suffix: ~

  metrics:
    rmse:
      type: cal_rmse_pt
      reduction: mean
      better: lower
    nse:
      type: cal_nse_pt
      reduction: mean
      better: higher
    nse_safe:
      type: cal_nse_pt_safe
      reduction: mean
      min_var: 1.0e-6
      abs_tol_per_px: 1.0e-4  # water depth diff less than 0.1mm -> perfect
      lower_bound: -5.0
      better: higher
    csi:
      type: cal_csi_pt
      threshold: 0.05
      reduction: mean
      better: higher
    precision:
      type: cal_precision_pt
      threshold: 0.05
      reduction: mean
      better: higher
    recall:
      type: cal_recall_pt
      threshold: 0.05
      reduction: mean
      better: higher
    target_prevalence:
      type: cal_prev_t_pt
      threshold: 0.05
      reduction: mean
    pred_prevalence:
      type: cal_prev_p_pt
      threshold: 0.05
      reduction: mean
